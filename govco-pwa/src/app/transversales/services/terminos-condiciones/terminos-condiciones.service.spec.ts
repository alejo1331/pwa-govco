/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { TerminosCondicionesService } from './terminos-condiciones.service';

describe('Service: TerminosCondiciones', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TerminosCondicionesService]
    });
  });

  it('should ...', inject([TerminosCondicionesService], (service: TerminosCondicionesService) => {
    expect(service).toBeTruthy();
  }));
});
