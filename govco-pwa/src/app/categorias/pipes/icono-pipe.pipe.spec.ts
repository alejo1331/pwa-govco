import { IconoPipePipe } from './icono-pipe.pipe';

describe('IconoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IconoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
