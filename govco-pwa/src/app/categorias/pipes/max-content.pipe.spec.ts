import { MaxContentPipe } from './max-content.pipe';

describe('MaxContentPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxContentPipe();
    expect(pipe).toBeTruthy();
  });
});
