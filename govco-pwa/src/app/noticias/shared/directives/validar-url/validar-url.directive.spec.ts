import { ValidarUrlDirective } from './validar-url.directive';

describe('ValidarUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarUrlDirective();
    expect(directive).toBeTruthy();
  });
});
